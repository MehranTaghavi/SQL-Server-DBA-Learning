# 02 - Sort and Filter

This section covers exercises for `WHERE`, conditional operators, `LIKE`, `IN`, `BETWEEN`, `ORDER BY`, `TOP`, and combined filtering.

The files `18-combined-practice.sql` and `19-combined-between-notlike.sql` are intentionally placed here, as they practice selecting columns along with multiple `WHERE` conditions and sorting simultaneously.
