# Common Table Expressions (CTE)

CTEs make multi-step analysis easier to read and maintain. The first exercise translates a salary-gap analysis into a named query pipeline.

Next additions will cover recursive CTEs, multiple CTEs, and when a CTE is preferable to a derived table or temporary table.

